apiVersion: v1 
kind: Service 
metadata:
  name: nginx-lb 
spec:
  type: LoadBalancer
  selector:
     project: roboshop 
     component: frontend 
     env: dev 
  ports:
    - protocol: TCP
      port: 80 # svc port
      targetPort: 80 #container port
      #nodePort: 32111
